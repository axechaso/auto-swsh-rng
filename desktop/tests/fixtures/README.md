`ocr-digits.png` 是固定的黑字白底数字 `123` 测试图（360 × 120）。使用 Windows Qt、Arial 40 px 居中绘制后保存；不需要在测试机器安装字体或重新绘图。

原测试每次调用 OpenCV 绘制数字，不同执行环境曾分别识别成 `123` 和 `122`。固定输入后继续断言精确文本与置信度 > 0.8，用于验证随包 Tesseract DLL 和 `chi_sim` 数据能真实识别；不代表游戏截图的识别准确率。
