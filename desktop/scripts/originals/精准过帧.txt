
_过几帧 = 填入这里			#这个如果用脚本默认用这个
_撞帧方向 = 1  				#1是向前  2是向后 3是向左 4是向右
_需要保存 = 1				#1是需要保存 0是不需要

#乱列阵兵用
_需要预留 = 0			#需要预留就填1不需要就填0
_预留帧数 = 40			#这里填你需要的帧数

#雷雨雨天乱数改再改下面内容
#当前月日是正常天气日期
_雷雨雨天乱数 = 0	#1是雷雨雨天乱数 其他数字是不启用
_雷雨雨天误差 = 4	#这里填入雷雨雨天误差
_当前月 = 8
_当前日 = 1
_雷雨月 = 11
_雷雨日 = 1

$2 = _雷雨雨天乱数
$3 = _当前月
$4 = _当前日
$5 = _雷雨月
$6 = _雷雨日
$7 = _雷雨雨天误差
$8 = _过几帧
$9 = _需要预留
$10 = _预留帧数
$11 = _需要保存

#唤醒手柄
FOR 5
    LCLICK 120
    100
NEXT

IF $2 = 1
    $8 -= $7
ENDIF
IF $9 = 1
    $8 -= $10
ENDIF
PRINT 开始摇杆过帧
FOR $8
    RCLICK
    100
NEXT
PRINT 过帧完毕
IF $2 = 1
    B 200
    2500
    HOME 50
    1000
    DOWN 100
    200
    FOR 7
        RIGHT 50
        50
    NEXT
    A 50
    1000
    LS DOWN,2000	#移动到主机位置
    400
    RIGHT 200
    200
    LS DOWN,700	#移动到日期位置
    200
    A 50
    200
    $3 -= $5
    $4 -= $6
    LS DOWN,550
    200
    A 200
    500
    RIGHT 200
    200
    IF $3 > 0
        FOR $3
            DOWN 200
            200
        NEXT
    ENDIF
    IF $3 < 0
        $3 = -$3
        FOR $3
            UP 200
            200
        NEXT
    ENDIF
    500
    RIGHT 200
    500
    IF $4 > 0
        FOR $4
            DOWN 200
            200
        NEXT
    ENDIF
    IF $4 < 0
        FOR $4
            $4 = -$4
            UP 200
            200
        NEXT
    ENDIF
    LS RIGHT,800
    400
    A 200
    400
    HOME 100
    3000
    A 100
    3000
ENDIF
$1 = _撞帧方向
IF $2 = 1
    PRINT 自动撞帧
    B
    500
ELSE
    IF $1 > 0
        PRINT 自动撞帧
        B
        1500
        B
        500
    ENDIF
ENDIF
IF $1 = 1
    LS UP,400
    LS RESET
    200
    X
    2000
    PRINT 宝可梦刷出，请手动检查
ENDIF
IF $1 = 2
    LS DOWN,400
    LS RESET
    200
    X
    2000
    PRINT 宝可梦刷出，请手动检查
ENDIF
IF $1 = 3
    LS LEFT,400
    LS RESET
    X
    2000
    PRINT 宝可梦刷出，请手动检查
ENDIF
IF $1 = 4
    LS RIGHT,400
    LS RESET
    X
    2000
    PRINT 宝可梦刷出，请手动检查
ENDIF
IF $11 = 1
    500
    R
    2000
    A
    200
ENDIF

