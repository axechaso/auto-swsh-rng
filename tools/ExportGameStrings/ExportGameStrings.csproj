<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <!-- The importing script reads this version from the Chinese owoow checkout. -->
    <PackageReference Include="PKHeX.Core" Version="$(PKHeXVersion)" />
  </ItemGroup>
</Project>
